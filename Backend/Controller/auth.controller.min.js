import User from"../Model/userDetails.model.js";import bcryptjs from"bcryptjs";import{errorHandler}from"../Utils/error.js";export const register=async(req,res,next)=>{const{uname:uname,email:email,password:password}=req.body,hashPassword=bcryptjs.hashSync(password,10);try{const emailExist=await User.findOne({email:email});emailExist&&next(errorHandler(500,"Email Exist")),await User.create({username:uname,email:email,password:hashPassword}),res.send({status:"Account Register"})}catch(error){next(errorHandler(500,"Oh no, something went wrong"))}};export const login=async(req,res)=>{const{email:email,password:password}=req.body;try{const user=await User.findOne({email:email});if(!user)return res.send({status:"No User Found"});if(password===user.password){const accToken=jwt.sign({},JWT_SECRET);return res.status(200)?res.send({status:"Account Login",accToken:accToken}):res.send({status:"Incorrect Details",accToken:accToken})}}catch(error){return res.send(error)}};